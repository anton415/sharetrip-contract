get:
  operationId: health
  summary: Check HTTP server availability
  responses:
    '200':
      description: HTTP server is running
      content:
        text/plain:
          schema:
            type: string
          example: OK
